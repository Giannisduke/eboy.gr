---
- name: Create Acorn cache directory writable by www-data
  file:
    path: "{{ deploy_helper.new_release_path }}/web/app/cache"
    state: directory
    owner: "{{ web_user }}"
    group: "{{ web_group }}"
    mode: "0775"
  become: true
