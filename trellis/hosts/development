# Add each host to the [development] group and to a "type" group such as [web] or [db].
# List each machine only once per [group], even if it will host multiple sites.

[development]
127.0.0.1 ansible_port=52226 ansible_user=eboy

[web]
127.0.0.1 ansible_port=52226 ansible_user=eboy
