# Netdata nginx stub_status endpoint (loopback only)
server {
  listen 127.0.0.1:{{ netdata_nginx_stub_status_port | default(81) }};
  server_name 127.0.0.1;

  access_log off;

  location = /stub_status {
    stub_status;
    allow 127.0.0.1;
    deny all;
  }
}