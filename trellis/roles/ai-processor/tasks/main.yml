---
# AI Product Processor Ansible Tasks
#
# Path conventions (Trellis):
#   site_root = {{ www_root }}/{{ site_key }}  (e.g. /srv/www/eboy.gr)
#   site_root/current/...  — current release (symlink); code lives here
#   site_root/shared/...   — persistent across deploys; .env, logs, xml live here

- name: Set site root from wordpress_sites
  set_fact:
    ai_site_root: "{{ www_root }}/{{ wordpress_sites.keys() | first }}"
  tags: [ai-processor, env, venv, dependencies, packages]

- name: Install Python prerequisites
  apt:
    name:
      - python3
      - python3-pip
      - python3-venv
      - python3-dev
      - build-essential
      - libjpeg-dev
      - zlib1g-dev
      - libwebp-dev
      - libpng-dev
      - libgomp1
    state: present
    update_cache: yes
    cache_valid_time: 3600
  tags: [ai-processor, packages]

- name: Create shared AI processor directories (persistent across deploys)
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ web_user }}"
    group: "{{ web_group }}"
    mode: '0755'
  loop:
    - "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/product-ai-processor"
    - "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/product-ai-processor/logs"
    - "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/xml_files"
    - "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/xml_files/en"
    - "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/xml_files/gr"
    - "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/xml_files/enhanced"
    - "{{ ai_site_root }}/shared/web/app/uploads/ai-processed-images"
  tags: [ai-processor]

- name: Deploy .env file for AI processor (shared — survives deploys)
  template:
    src: env.j2
    dest: "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/product-ai-processor/.env"
    owner: "{{ web_user }}"
    group: "{{ web_group }}"
    mode: '0640'
  tags: [ai-processor, env]

- name: Symlink .env into current release
  file:
    src: "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/product-ai-processor/.env"
    dest: "{{ ai_site_root }}/current/web/app/themes/simple-city/scripts/product-ai-processor/.env"
    state: link
    owner: "{{ web_user }}"
    group: "{{ web_group }}"
    force: yes
  tags: [ai-processor, env]

- name: Check if AI processor code is deployed
  stat:
    path: "{{ ai_site_root }}/current/web/app/themes/simple-city/scripts/product-ai-processor/requirements.txt"
  register: ai_processor_code
  tags: [ai-processor, venv, dependencies]

- name: Ensure Python venv exists (in shared — persists across deploys)
  command: python3 -m venv "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/product-ai-processor/venv"
  args:
    creates: "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/product-ai-processor/venv/bin/python3"
  become_user: "{{ web_user }}"
  when: ai_processor_code.stat.exists
  tags: [ai-processor, venv]

- name: Remove existing venv directory from current release (if not already a symlink)
  file:
    path: "{{ ai_site_root }}/current/web/app/themes/simple-city/scripts/product-ai-processor/venv"
    state: absent
  when: ai_processor_code.stat.exists
  tags: [ai-processor, venv]

- name: Symlink venv into current release
  file:
    src: "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/product-ai-processor/venv"
    dest: "{{ ai_site_root }}/current/web/app/themes/simple-city/scripts/product-ai-processor/venv"
    state: link
    owner: "{{ web_user }}"
    group: "{{ web_group }}"
    force: yes
  when: ai_processor_code.stat.exists
  tags: [ai-processor, venv]

- name: Install Python dependencies
  pip:
    requirements: "{{ ai_site_root }}/current/web/app/themes/simple-city/scripts/product-ai-processor/requirements.txt"
    virtualenv: "{{ ai_site_root }}/shared/web/app/themes/simple-city/scripts/product-ai-processor/venv"
    virtualenv_command: python3 -m venv
  become_user: "{{ web_user }}"
  when: ai_processor_code.stat.exists
  tags: [ai-processor, dependencies]

- name: Display AI processor deployment status
  debug:
    msg: "{{ 'AI processor Python dependencies installed successfully' if ai_processor_code.stat.exists else 'AI processor code not yet deployed - run deploy.yml to install Python dependencies' }}"
  tags: [ai-processor]
