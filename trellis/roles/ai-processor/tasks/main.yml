---
# AI Product Processor Ansible Tasks

- name: Install Python prerequisites
  apt:
    name:
      - python3
      - python3-pip
      - python3-venv
      - python3-dev
      - build-essential
      - libjpeg-dev
      - zlib1g-dev
      - libwebp-dev
      - libpng-dev
      - libgomp1
    state: present
    update_cache: yes
    cache_valid_time: 3600
  tags: [ai-processor, packages]

- name: Create AI processor directories
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ web_user }}"
    group: "{{ web_group }}"
    mode: '0755'
  loop:
    - "{{ www_root }}/web/app/themes/simple-city/scripts/product-ai-processor/logs"
    - "{{ www_root }}/web/app/themes/simple-city/scripts/product-ai-processor/output"
    - "{{ www_root }}/web/app/themes/simple-city/scripts/xml_files/en"
    - "{{ www_root }}/web/app/themes/simple-city/scripts/xml_files/gr"
    - "{{ www_root }}/web/app/themes/simple-city/scripts/xml_files/enhanced"
    - "{{ www_root }}/web/app/uploads/ai-processed-images"
  tags: [ai-processor]

- name: Deploy .env file for AI processor
  template:
    src: env.j2
    dest: "{{ www_root }}/web/app/themes/simple-city/scripts/product-ai-processor/.env"
    owner: "{{ web_user }}"
    group: "{{ web_group }}"
    mode: '0640'
  tags: [ai-processor, env]

- name: Check if AI processor code is deployed
  stat:
    path: "{{ www_root }}/web/app/themes/simple-city/scripts/product-ai-processor/requirements.txt"
  register: ai_processor_code
  tags: [ai-processor, venv, dependencies]

- name: Ensure Python venv exists
  command: python3 -m venv venv
  args:
    chdir: "{{ www_root }}/web/app/themes/simple-city/scripts/product-ai-processor"
    creates: "{{ www_root }}/web/app/themes/simple-city/scripts/product-ai-processor/venv"
  become_user: "{{ web_user }}"
  when: ai_processor_code.stat.exists
  tags: [ai-processor, venv]

- name: Install Python dependencies
  pip:
    requirements: "{{ www_root }}/web/app/themes/simple-city/scripts/product-ai-processor/requirements.txt"
    virtualenv: "{{ www_root }}/web/app/themes/simple-city/scripts/product-ai-processor/venv"
    virtualenv_command: python3 -m venv
  become_user: "{{ web_user }}"
  when: ai_processor_code.stat.exists
  tags: [ai-processor, dependencies]

- name: Display AI processor deployment status
  debug:
    msg: "{{ 'AI processor Python dependencies installed successfully' if ai_processor_code.stat.exists else 'AI processor code not yet deployed - run deploy.yml to install Python dependencies' }}"
  tags: [ai-processor]
