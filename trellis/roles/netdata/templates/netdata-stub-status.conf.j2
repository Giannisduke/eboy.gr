server {
  listen 127.0.0.1:81;
  server_name 127.0.0.1;

  access_log off;

  location = {{ netdata_nginx_stub_status_path | default('/stub_status') }} {
    stub_status;
    allow 127.0.0.1;
    deny all;
  }
}