/**
 * Theme scripts.
 *
 * Imports the full Bootstrap 5.3 JavaScript bundle (dropdowns, modals,
 * tooltips, offcanvas, etc.). Popper is pulled in automatically via the
 * @popperjs/core dependency. To trim the bundle, import only the
 * components you use, e.g.:
 *
 *   import { Tooltip, Dropdown } from 'bootstrap';
 */
import 'bootstrap';
