jQuery(document).ready(function($) {
  $(".owl-carousel").owlCarousel({
    items:3,
    loop:true,
    margin:5,
    autoplay:true,
    autoWidth: true,
    autoplayTimeout:100,
    autoplayHoverPause:true
});

});

